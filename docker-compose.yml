# Local development stack.
#
# The `api` service is added in a later phase, once the Dockerfile exists.
# Right now this gives you a Postgres with pgvector already compiled in, which
# is what the app and the test suite point at by default.
#
#   docker compose up -d db

services:
  db:
    # pgvector's own image rather than the stock `postgres` one: the extension
    # is a C extension that has to be compiled against the server, so
    # `CREATE EXTENSION vector` fails on a plain postgres image.
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: app
    ports:
      # Host 5432 -> container 5432. Change the host side if you already run a
      # local Postgres, and update DATABASE_URL to match.
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      # Anything in this directory runs on FIRST boot only. If you change the
      # init script later you must `docker compose down -v` to wipe the volume,
      # or it will never run again.
      - ./docker/postgres-init.sql:/docker-entrypoint-initdb.d/00-init.sql:ro
    healthcheck:
      # `-U app -d app` because pg_isready defaults to the OS user (root),
      # which does not exist as a Postgres role here and would report failure
      # on a perfectly healthy server.
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 5s
      timeout: 3s
      # Generous: the very first boot also runs initdb and the init script.
      retries: 20

volumes:
  # Named volume, so `docker compose down` keeps your data. Use
  # `docker compose down -v` when you actually want a clean database.
  pgdata:
